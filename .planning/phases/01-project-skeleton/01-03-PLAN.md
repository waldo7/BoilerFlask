---
phase: 01-project-skeleton
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - app/templates/base.html
  - app/templates/home.html
  - app/templates/error.html
  - app/templates/about.html
  - app/templates/contact.html
autonomous: true
requirements:
  - MAIN-01
  - LAYOUT-01
  - LAYOUT-02
  - MOB-01
  - MOB-03

must_haves:
  truths:
    - "base.html includes Bootstrap 5.3.8 CSS CDN link with integrity hash"
    - "base.html includes Bootstrap Icons 1.13.1 CSS CDN link"
    - "base.html includes Bootstrap 5.3.8 JS bundle CDN at end of body per D-16"
    - "base.html has <meta name=viewport> tag per D-13"
    - "base.html renders a sidebar block containing both desktop fixed sidebar and mobile offcanvas"
    - "base.html sidebar brand shows 'FlaskStuct' + bi-house icon per D-06"
    - "base.html sidebar links Dashboard and Settings per D-05"
    - "base.html sidebar links use request.endpoint for .active class per D-08"
    - "home.html extends base.html and displays 'Flask app is running' per MAIN-01"
    - "error.html is a standalone template that does NOT extend base.html per D-09"
    - "error.html renders code, title, message, icon variables passed from handlers"
    - "error.html has a 'Go Home' button linking to main.index per D-10"
    - "about.html and contact.html extend base.html for sidebar .active class testing"
    - "mobile hamburger button visible only on mobile (d-md-none) per D-03"
    - "desktop sidebar hidden on mobile, visible on desktop per D-03"
  artifacts:
    - path: "app/templates/base.html"
      provides: "Bootstrap 5 layout with responsive sidebar"
      min_lines: 80
    - path: "app/templates/home.html"
      provides: "Homepage with Flask app is running"
      min_lines: 8
    - path: "app/templates/error.html"
      provides: "Standalone parameterized error page"
      min_lines: 30
    - path: "app/templates/about.html"
      provides: "Placeholder for sidebar active class testing"
      min_lines: 5
    - path: "app/templates/contact.html"
      provides: "Placeholder for sidebar active class testing"
      min_lines: 5
  key_links:
    - from: "app/templates/base.html"
      to: "CDN"
      via: "<link> and <script> tags"
      pattern: "cdn\\.jsdelivr\\.net.*bootstrap.*5\\.3\\.8"
    - from: "app/templates/base.html"
      to: "app/static/css/app.css"
      via: "url_for static"
      pattern: "url_for.*static.*app\\.css"
    - from: "app/templates/home.html"
      to: "app/templates/base.html"
      via: "Jinja extends"
      pattern: "{% extends.*base\\.html"
    - from: "app/templates/error.html"
      to: "app/__init__.py error handlers"
      via: "Jinja variables code, title, message, icon"
      pattern: "\\{\\{\\s*(code|title|message|icon)\\s*\\}\\}"
    - from: "app/templates/error.html"
      to: "homepage"
      via: "Go Home button url_for"
      pattern: "url_for.*main\\.index"
---

<objective>
Create all Jinja2 templates: the base layout with Bootstrap 5 CDN and dual-element sidebar, the homepage placeholder, the standalone error page, and placeholder about/contact pages for sidebar navigation testing.

Purpose: Implement all 16 locked decisions from CONTEXT.md that manifest in template files. The base.html is the central layout contract that all future pages will extend. Error.html provides branded, friendly error pages that render independently of the base layout.
Output: Five template files in app/templates/ — base.html, home.html, error.html, about.html, contact.html
</objective>

<execution_context>
@C:/Users/Waldo/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Waldo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-project-skeleton/01-CONTEXT.md
@.planning/phases/01-project-skeleton/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create app/templates/base.html — Bootstrap 5 layout with dual-element sidebar</name>
  <files>app/templates/base.html</files>
  <read_first>
    - @.planning/phases/01-project-skeleton/01-RESEARCH.md — Section: "Template Architecture" with full specifications for base.html including: Jinja block layout (head/title/content/sidebar/scripts), exact CDN URLs with integrity hashes, viewport meta tag, sidebar dual-element architecture (desktop fixed + mobile offcanvas), hamburger button, active page detection via request.endpoint, and script placement at end of body.
    - @.planning/phases/01-project-skeleton/01-CONTEXT.md — All D-01 through D-16 decisions. Key for this task: D-01 (blocks), D-02 (sidebar in base), D-03 (fixed >=768px, offcanvas <768px), D-04 (CDN only), D-05 (Dashboard + Settings links), D-06 (FlaskStuct + bi-house), D-08 (active class), D-13 (viewport meta + responsive grid), D-15 (bootstrap.bundle.min.js), D-16 (scripts at end of body).
    - @.planning/research/PITFALLS.md §5 — Mobile nav: include JS bundle, viewport meta, test at breakpoints.
  </read_first>
  <action>
    Create directory `app/templates/` if it does not exist. Create `app/templates/base.html`.

    Per D-01: define four core Jinja blocks — `head`, `content`, `sidebar`, `scripts`. Optionally include `title` as a nested block inside `head`. No intermediate layout templates — all pages extend `base.html` directly.

    **<head> block (D-04, D-13):**
    - `<meta charset="utf-8">`
    - `<meta name="viewport" content="width=device-width, initial-scale=1">` — required for responsive offcanvas per Bootstrap docs
    - `<title>` block defaulting to "FlaskStuct"
    - Bootstrap 5.3.8 CSS CDN: `<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-sRIl4kxILFvY47J16cr9ZwB07vP4J8+LH7qKQnuqkuIAvNWLzeN8tE5YBujZqJLB" crossorigin="anonymous">`
    - Bootstrap Icons 1.13.1 CDN: `<link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.13.1/font/bootstrap-icons.min.css" rel="stylesheet">`
    - Custom CSS: `<link rel="stylesheet" href="{{ url_for('static', filename='css/app.css') }}">`

    **Body structure (Bootstrap grid):**
    - Row with the hamburger button (mobile only, `d-md-none`) per D-03. Button has `data-bs-toggle="offcanvas" data-bs-target="#mobileSidebar"` and displays `navbar-toggler-icon` span.
    - Desktop fixed sidebar column: `<div class="sidebar-desktop d-none d-md-flex flex-column">` — contains brand header and nav links (see below).
    - Content column: `<div class="content-col">` with `{% block content %}{% endblock %}`. This column gets `margin-left: var(--sidebar-width)` from app.css.
    - Mobile offcanvas sidebar: `<div class="offcanvas offcanvas-start" id="mobileSidebar">` — contains the same brand header and nav links as desktop sidebar.

    **Sidebar content (shared between desktop and offcanvas) per D-02, D-05, D-06, D-08:**
    - **Brand header (D-06):** `<div class="sidebar-brand">` with `<i class="bi bi-house"></i> FlaskStuct`
    - **Navigation links (D-05):** unordered list with nav-link classes:
      - Dashboard link: `href="{{ url_for('main.index') }}"` with `{% if request.endpoint == 'main.index' %}active{% endif %}` per D-08
      - Settings link: `href="{{ url_for('main.index') }}"` with `{% if request.endpoint == 'main.index' %}active{% endif %}` — links to homepage as placeholder until wired in Phase 3
    - Per D-07: NO admin-only links. Only Dashboard and Settings.

    **scripts block (D-15, D-16):**
    - TODO comment: "Phase 2: Add extension JS, custom scripts here"
    - Bootstrap 5.3.8 JS Bundle CDN: `<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js" integrity="sha384-FKyoEForCGlyvwx9Hj09JcYn3nv7wiPVlz7YYwJrWVcXK/BmnVDxM+D2scQbITxI" crossorigin="anonymous"></script>` — includes Popper, required for offcanvas component per D-15.
    - Placed at end of `<body>` per D-16.

    The sidebar block `{% block sidebar %}` wraps both desktop and offcanvas elements so pages can override sidebar content if needed in Phase 3. The mobile offcanvas uses Bootstrap's `offcanvas-start` class for left-edge slide-in. The desktop sidebar uses `d-none d-md-flex` to be hidden on mobile, visible on >=768px. Per RESEARCH.md "Don't Hand-Roll": the offcanvas JS/accessibility/backdrop/scroll-lock is all handled by Bootstrap.
  </action>
  <verify>
    <automated>python -c "
from flask import Flask, render_template_string
import os
os.chdir('app')
app = Flask(__name__)
with app.app_context():
    html = render_template_string(open('templates/base.html').read())
    assert 'bootstrap.min.css' in html, 'Missing Bootstrap CSS CDN'
    assert 'bootstrap.bundle.min.js' in html, 'Missing Bootstrap JS CDN'
    assert 'bootstrap-icons' in html, 'Missing Bootstrap Icons CDN'
    assert 'viewport' in html, 'Missing viewport meta'
    assert 'width=device-width' in html, 'Missing viewport width'
    assert 'FlaskStuct' in html, 'Missing app name'
    assert 'bi-house' in html, 'Missing brand icon'
    assert 'Dashboard' in html, 'Missing Dashboard link'
    assert 'Settings' in html, 'Missing Settings link'
    assert 'sidebar-desktop' in html, 'Missing desktop sidebar'
    assert 'offcanvas' in html, 'Missing offcanvas for mobile'
    assert 'mobileSidebar' in html, 'Missing mobileSidebar id'
    assert 'navbar-toggler-icon' in html, 'Missing hamburger icon'
    assert 'd-md-none' in html, 'Missing mobile-only visibility class'
    assert 'd-none d-md-flex' in html, 'Missing desktop visibility class'
    assert 'content-col' in html, 'Missing content-col class'
    assert 'app.css' in html, 'Missing custom CSS link'
    assert 'integrity=' in html, 'Missing integrity hashes'
    print('base.html validation PASSED')
"</automated>
  </verify>
  <done>base.html exists with four Jinja blocks (head, content, sidebar, scripts). Loads Bootstrap 5.3.8 CSS + Icons + JS Bundle from CDN with integrity hashes. Viewport meta tag present. Sidebar renders both desktop fixed (d-none d-md-flex) and mobile offcanvas (#mobileSidebar with offcanvas-start). Brand header shows "FlaskStuct" + bi-house icon (D-06). Dashboard and Settings links use request.endpoint for .active class (D-08). No admin links (D-07). Scripts at end of body (D-16). Custom CSS loaded via url_for('static').</done>
</task>

<task type="auto">
  <name>Task 2: Create app/templates/home.html, about.html, and contact.html</name>
  <files>app/templates/home.html, app/templates/about.html, app/templates/contact.html</files>
  <read_first>
    - @.planning/phases/01-project-skeleton/01-RESEARCH.md — Section: "Homepage Template" (exact Jinja structure for home.html), "Placeholder Templates for Sidebar Active Class Testing" (exact structure for about.html, contact.html)
    - @.planning/phases/01-project-skeleton/01-CONTEXT.md — D-01: all pages extend base.html directly. D-08: sidebar .active class detection.
    - ROADMAP.md Phase 1 Success Criteria: #2 "Visiting / displays homepage with 'Flask app is running'"
  </read_first>
  <action>
    Create three templates, all extending `base.html` per D-01.

    **home.html** (MAIN-01): Extends "base.html". Override `title` block with "FlaskStuct - Home". Override `content` block with a `<div class="container-fluid py-4">` containing `<h1>FlaskStuct</h1>` and `<p class="lead">Flask app is running.</p>`. This is the key verification target for MAIN-01 — the "Flask app is running" message proves the scaffold works.

    **about.html**: Extends "base.html". Override `title` block with "About - FlaskStuct". Override `content` block with `<h1>About</h1><p>Placeholder about page.</p>` inside a container. Minimal content — exists to provide a distinct endpoint for sidebar .active class detection per D-08.

    **contact.html**: Extends "base.html". Override `title` block with "Contact - FlaskStuct". Override `content` block with `<h1>Contact</h1><p>Placeholder contact page.</p>` inside a container. Same purpose — distinct endpoint for sidebar navigation testing.
  </action>
  <verify>
    <automated>python -c "
assert open('app/templates/home.html').read().count('Flask app is running') >= 1, 'Missing Flask app is running in home.html'
assert '{% extends \"base.html\" %}' in open('app/templates/home.html').read(), 'home.html does not extend base'
assert '{% extends \"base.html\" %}' in open('app/templates/about.html').read(), 'about.html does not extend base'
assert '{% extends \"base.html\" %}' in open('app/templates/contact.html').read(), 'contact.html does not extend base'
assert 'About' in open('app/templates/about.html').read(), 'about.html missing About heading'
assert 'Contact' in open('app/templates/contact.html').read(), 'contact.html missing Contact heading'
print('Page templates OK')
"</automated>
  </verify>
  <done>home.html extends base.html, displays "Flask app is running" in lead paragraph (MAIN-01). about.html and contact.html extend base.html with minimal placeholder content — distinct titles and headings for sidebar .active class detection (D-08).</done>
</task>

<task type="auto">
  <name>Task 3: Create app/templates/error.html — standalone parameterized error page</name>
  <files>app/templates/error.html</files>
  <read_first>
    - @.planning/phases/01-project-skeleton/01-RESEARCH.md — Section: "Error Template" with full specification: standalone HTML document (no extends base.html), viewport meta, Bootstrap CDN, minimal inline centering CSS, icon display, code/title/message variables, Go Home button.
    - @.planning/phases/01-project-skeleton/01-CONTEXT.md — D-09: error pages are standalone minimal templates, do NOT extend base.html. D-10: friendly tone + Go Home button. D-11: app name + relevant Bootstrap icon. D-12: single error.html shared across all error codes, accepts code/title/message/icon as variables.
    - @.planning/research/PITFALLS.md §7 — Missing error handlers: error pages should not depend on base layout to avoid compounding errors.
  </read_first>
  <action>
    Create `app/templates/error.html` as a standalone HTML document per D-09. This template does NOT use `{% extends "base.html" %}` — it contains its own complete `<html>`, `<head>`, and `<body>`.

    **<head>:**
    - Same charset and viewport meta as base.html
    - `<title>{{ title }} - FlaskStuct</title>` — uses the `title` variable from error handlers per D-11, D-12
    - Bootstrap 5.3.8 CSS CDN link (same URL + integrity hash as base.html)
    - Bootstrap Icons 1.13.1 CDN link (same URL as base.html)
    - Inline `<style>` block for error page centering: body with `display:flex; align-items:center; justify-content:center; min-height:100vh; background-color:#f8f9fa;`, .error-container with `text-align:center; padding:2rem;`

    **<body>:**
    - `<div class="error-container">` containing:
      - `<i class="{{ icon }} display-1 text-muted mb-3"></i>` — displays the Bootstrap icon from error handlers per D-11 (bi-shield-lock, bi-question-circle, or bi-exclamation-triangle)
      - `<h1>{{ code }}</h1>` — displays the HTTP status code
      - `<h2 class="h4 text-muted">{{ title }}</h2>` — displays the error title per D-10 (friendly, helpful tone)
      - `<p class="lead">{{ message }}</p>` — displays the error message per D-10
      - `<a href="{{ url_for('main.index') }}" class="btn btn-primary btn-lg mt-3"><i class="bi-house"></i> Go Home</a>` — per D-10: clear "Go Home" button with house icon

    All four variables (code, title, message, icon) are passed from the `register_error_handlers()` function in app/__init__.py per D-12. The template does NOT load Bootstrap JS bundle — error pages don't need offcanvas or interactive components.
  </action>
  <verify>
    <automated>python -c "
html = open('app/templates/error.html').read()
assert '{% extends' not in html, 'ERROR: error.html extends base.html — violates D-09. Must be standalone.'
assert '{{ code }}' in html, 'Missing code variable'
assert '{{ title }}' in html, 'Missing title variable'
assert '{{ message }}' in html, 'Missing message variable'
assert '{{ icon }}' in html, 'Missing icon variable'
assert 'Go Home' in html, 'Missing Go Home button (D-10)'
assert 'url_for' in html, 'Missing url_for for Go Home link (D-10)'
assert 'bi-house' in html, 'Missing Go Home icon'
assert 'FlaskStuct' in html, 'Missing app name (D-11)'
assert 'bootstrap.min.css' in html, 'Missing Bootstrap CSS CDN'
assert 'viewport' in html, 'Missing viewport meta'
assert 'error-container' in html, 'Missing error container div'
print('error.html validation PASSED')
"</automated>
  </verify>
  <done>error.html is a standalone HTML document (does NOT extend base.html per D-09). Contains viewport meta, Bootstrap CSS + Icons CDNs, inline centering CSS, and renders {{ code }}, {{ title }}, {{ message }}, {{ icon }} variables passed from error handlers (D-12). Shows app name + relevant icon (D-11). Has friendly messaging with "Go Home" button linking to main.index (D-10). No Bootstrap JS bundle — not needed for static error display.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CDN→browser | Bootstrap CSS/JS loaded from external CDN |
| client→server | Template rendering on error conditions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Tampering | CDN (Bootstrap CSS/JS) | mitigate | Integrity hashes on `<link>` and `<script>` tags per RESEARCH.md — browser refuses to load if file content doesn't match expected hash. Verified against official Bootstrap 5.3.8 docs. |
| T-01-06 | Information Disclosure | error.html | mitigate | Error template renders friendly messages without internal details. No stack traces, no file paths, no exception messages in the HTML output. The `code` variable shows HTTP status codes only (403/404/500) — standard information, not sensitive. |
</threat_model>

<verification>
Verify templates parse without Jinja errors (requires app context): `python -c "from app import create_app; app = create_app('testing'); with app.app_context(): from flask import render_template; render_template('base.html'); print('base.html renders OK')"`
(Expected to fail until Plan 02 creates app/__init__.py — run after Plan 02 executes.)
</verification>

<success_criteria>
- [ ] base.html loads Bootstrap 5.3.8 CSS + Icons + JS Bundle from CDN with integrity hashes
- [ ] base.html viewport meta tag present per D-13
- [ ] base.html sidebar renders desktop fixed (d-none d-md-flex) and mobile offcanvas (#mobileSidebar)
- [ ] base.html sidebar brand shows "FlaskStuct" + bi-house icon per D-06
- [ ] base.html sidebar links Dashboard and Settings with request.endpoint .active detection per D-05, D-08
- [ ] home.html extends base.html, displays "Flask app is running" per MAIN-01
- [ ] error.html is standalone (no extends base.html) per D-09, renders code/title/message/icon per D-12
- [ ] error.html "Go Home" button links to main.index per D-10
- [ ] about.html and contact.html extend base.html for sidebar .active testing
</success_criteria>

<output>
Create `.planning/phases/01-project-skeleton/01-03-SUMMARY.md` when done
</output>
