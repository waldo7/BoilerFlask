---
phase: 01-project-skeleton
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - requirements.txt
  - .env.example
  - config.py
  - tests/conftest.py
  - tests/test_main.py
  - tests/test_layout.py
  - tests/test_errors.py
autonomous: true
requirements:
  - MAIN-01
  - LAYOUT-01
  - LAYOUT-02
  - MOB-03

must_haves:
  truths:
    - "requirements.txt pins flask==3.1.3 and python-dotenv==1.2.2"
    - ".env.example shows SECRET_KEY template without real secrets"
    - "config.py has Config base class with env-var-driven settings"
    - "tests/conftest.py provides a Flask test client fixture using TestingConfig"
    - "A written test verifies homepage returns 200 with 'Flask app is running'"
    - "A written test verifies base template includes Bootstrap CDN CSS link"
    - "A written test verifies viewport meta tag exists in base template"
    - "A written test verifies 404 response renders styled error page with 'Page Not Found'"
    - "pytest is installed and importable"
  artifacts:
    - path: "requirements.txt"
      provides: "Pinned pip dependencies"
      min_lines: 2
    - path: "config.py"
      provides: "Configuration class hierarchy"
      min_lines: 15
    - path: "tests/conftest.py"
      provides: "Flask test client fixture"
      min_lines: 10
    - path: "tests/test_main.py"
      provides: "MAIN-01 homepage test"
      min_lines: 8
    - path: "tests/test_layout.py"
      provides: "LAYOUT-01 + MOB-03 template tests"
      min_lines: 12
    - path: "tests/test_errors.py"
      provides: "LAYOUT-02 error page tests"
      min_lines: 12
  key_links:
    - from: "tests/conftest.py"
      to: "config.py"
      via: "TestingConfig import"
      pattern: "from config import TestingConfig"
    - from: "tests/test_main.py"
      to: "app.create_app"
      via: "test client fixture"
      pattern: "create_app.*testing"
    - from: "tests/test_errors.py"
      to: "error handler"
      via: "Flask test client get"
      pattern: "client.get.*nonexistent"
---

<objective>
Create all zero-dependency project configuration files and the test scaffold.

Purpose: Establish the foundation files (dependency declarations, config, gitignore, env template) that all subsequent plans depend on, and create the automated test suite that will validate the scaffold once built.
Output: requirements.txt, .gitignore, .env.example, config.py, and a complete pytest test suite with 4 test files.
</objective>

<execution_context>
@C:/Users/Waldo/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Waldo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-project-skeleton/01-CONTEXT.md
@.planning/phases/01-project-skeleton/01-RESEARCH.md
@.planning/phases/01-project-skeleton/01-VALIDATION.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create requirements.txt, .gitignore, and .env.example</name>
  <files>requirements.txt, .gitignore, .env.example</files>
  <read_first>
    - @.planning/phases/01-project-skeleton/01-RESEARCH.md — Sections: "requirements.txt" (exact version pins), ".env.example" (exact template), "Package Legitimacy Audit" (approved packages), "file manifest wave 0"
    - @.planning/research/PITFALLS.md §6 — Hardcoded config: env var pattern for secrets
  </read_first>
  <action>
    **requirements.txt:** Create with two pinned dependencies per RESEARCH.md §1 Component Specifications: `Flask==3.1.3` and `python-dotenv==1.2.2`, each on its own line. These are the only two runtime dependencies needed for Phase 1. No other packages.

    **.gitignore:** Create with entries for: `venv/`, `instance/`, `.env`, `__pycache__/`, `*.pyc`, `.planning/`. Ensures virtual environments, Flask instance folder (where SQLite db will live), environment secrets, Python bytecode, and planning artifacts are never committed per PITFALLS.md §6.

    **.env.example:** Create with four entries per RESEARCH.md §2: `SECRET_KEY=change-me-to-a-random-secret`, `FLASK_ENV=development`, `FLASK_DEBUG=1`, `DATABASE_URL=sqlite:///instance/app.db`. This is the template file that gets copied — never holds real secrets. The real `.env` is in .gitignore.

    All three files are created at project root.
  </action>
  <verify>
    <automated>python -c "open('requirements.txt'); lines = open('requirements.txt').readlines(); assert any('Flask==3.1.3' in l for l in lines); assert any('python-dotenv==1.2.2' in l for l in lines); print('requirements.txt OK')" &amp;&amp; python -c "assert 'venv/' in open('.gitignore').read(); assert '.env' in open('.gitignore').read(); print('.gitignore OK')" &amp;&amp; python -c "assert 'SECRET_KEY=' in open('.env.example').read(); assert 'FLASK_ENV=' in open('.env.example').read(); print('.env.example OK')"</automated>
  </verify>
  <done>requirements.txt has two pinned packages. .gitignore excludes venv, instance, .env, __pycache__, .planning/. .env.example has SECRET_KEY, FLASK_ENV, FLASK_DEBUG, DATABASE_URL entries.</done>
</task>

<task type="auto">
  <name>Task 2: Create config.py with configuration class hierarchy</name>
  <files>config.py</files>
  <read_first>
    - @.planning/phases/01-project-skeleton/01-RESEARCH.md — Section: "config.py" with full Component Specification (Config base class, DevelopmentConfig, ProductionConfig, TestingConfig). Key decisions: SECRET_KEY from os.environ.get with dev fallback, SQLALCHEMY_DATABASE_URI defaulting to SQLite, load_dotenv() at module level.
    - @.planning/research/PITFALLS.md §6 — Hardcoded config: never commit real secrets, use os.environ.get() with fallback for dev only.
    - @.planning/research/ARCHITECTURE.md — Config classes as foundational block.
  </read_first>
  <action>
    Create `config.py` at project root. Import `os` and `load_dotenv` from `dotenv`. Call `load_dotenv()` at module level so env vars are available when class bodies evaluate.

    **Config (base class):** Define four attributes: `SECRET_KEY = os.environ.get('SECRET_KEY', 'dev-secret-change-in-production')`, `SQLALCHEMY_DATABASE_URI = os.environ.get('DATABASE_URL', 'sqlite:///instance/app.db')`, `SQLALCHEMY_TRACK_MODIFICATIONS = False`, `WTF_CSRF_ENABLED = True`. Per PITFALLS.md §6: the dev fallback exists for zero-config local development only; production MUST set the env var.

    **DevelopmentConfig(Config):** Set `DEBUG = True` and `FLASK_ENV = 'development'`.

    **ProductionConfig(Config):** Set `DEBUG = False` and `FLASK_ENV = 'production'`.

    **TestingConfig(Config):** Set `TESTING = True`, `SQLALCHEMY_DATABASE_URI = 'sqlite:///:memory:'`, `WTF_CSRF_ENABLED = False`. Memory database ensures test isolation.

    Config selection logic (mapping FLASK_ENV string to class) lives in `create_app()` (Plan 02), NOT in this file. config.py only defines the classes.
  </action>
  <verify>
    <automated>python -c "compile(open('config.py').read(), 'config.py', 'exec'); print('config.py syntax OK')" &amp;&amp; python -c "content = open('config.py').read(); assert 'class Config' in content; assert 'class DevelopmentConfig' in content; assert 'class ProductionConfig' in content; assert 'class TestingConfig' in content; assert 'SECRET_KEY' in content; assert 'SQLALCHEMY_DATABASE_URI' in content; assert 'SQLALCHEMY_TRACK_MODIFICATIONS' in content; assert 'WTF_CSRF_ENABLED' in content; assert 'os.environ.get' in content; assert ':memory:' in content; print('config.py structure OK')"</automated>
  </verify>
  <done>Four config classes exist at module level with required attributes. SECRET_KEY uses os.environ.get with dev fallback per D-06 constraint and PITFALLS.md §6. TestingConfig uses in-memory SQLite.</done>
</task>

<task type="auto">
  <name>Task 3: Create test scaffold — conftest.py, test files, and install pytest</name>
  <files>tests/conftest.py, tests/test_main.py, tests/test_layout.py, tests/test_errors.py</files>
  <read_first>
    - @.planning/phases/01-project-skeleton/01-VALIDATION.md — Section: "Test Infrastructure" (pytest framework, commands), "Per-Task Verification Map" (test function names: test_homepage, test_base_template_has_bootstrap, test_viewport_meta, test_404_page), "Wave 0 Requirements" (5 items to create)
    - @.planning/phases/01-project-skeleton/01-RESEARCH.md — Section: "Validation Architecture" > "Phase Requirements → Test Map" (test types, automated commands), "TestingConfig" details
    - @.planning/research/PITFALLS.md — General awareness of Phase 1 pitfalls for test design
  </read_first>
  <action>
    **Step 1: Create tests/ directory** at project root. Then create four test files and install pytest.

    **tests/conftest.py:** Import pytest. Import `create_app` from `app` (this import will fail until Plan 02 creates app/__init__.py — that is expected and acceptable). Define a `@pytest.fixture` named `app` that calls `create_app('testing')` and yields the app. Define a `@pytest.fixture` named `client` that calls `app.test_client()` and returns it. Use `with app.app_context():` pattern for any context-dependent operations. These fixtures enable every other test file.

    **tests/test_main.py:** Import nothing (uses conftest fixtures). Write `test_homepage(client)` that calls `response = client.get('/')`, asserts `response.status_code == 200`, asserts `b'Flask app is running' in response.data`. Covering MAIN-01.

    **tests/test_layout.py:** Write two test functions:
    - `test_base_template_has_bootstrap(client)`: GET `/`, assert `response.status_code == 200`, assert `b'bootstrap.min.css' in response.data` (proves Bootstrap 5 CDN CSS is linked), assert `b'bootstrap.bundle.min.js' in response.data` (proves Bootstrap JS bundle is included). Covering LAYOUT-01.
    - `test_viewport_meta(client)`: GET `/`, assert `response.status_code == 200`, assert `b'viewport' in response.data`, assert `b'width=device-width' in response.data`. Covering MOB-03 structural responsiveness.

    **tests/test_errors.py:** Write one test function `test_404_page(client)`: GET a known-nonexistent path like `/nonexistent-page-12345`, assert `response.status_code == 404`, assert `b'Page Not Found' in response.data` (friendly message per D-10), assert `b'Go Home' in response.data` (Go Home button per D-10). Optionally also test 403 and 500: `test_403_page(client)` GET `/static/` (Flask returns 403 for directory listing in debug=False), `test_500_page(client)` — NOTE: 500 test requires triggering a server-side error; test existence of error handler registration rather than triggering the error if it complicates the test.

    **Step 2: Install pytest.** Run `pip install pytest`. If `pip install pytest` succeeds, verify importability. Do NOT add pytest to requirements.txt — it is a dev dependency and not required for the app to run.
  </action>
  <verify>
    <automated>python -c "import pytest; print('pytest installed OK')" &amp;&amp; python -c "assert open('tests/conftest.py'); compile(open('tests/conftest.py').read(), 'conftest.py', 'exec'); print('conftest.py syntax OK')" &amp;&amp; python -c "compile(open('tests/test_main.py').read(), 'test_main.py', 'exec'); compile(open('tests/test_layout.py').read(), 'test_layout.py', 'exec'); compile(open('tests/test_errors.py').read(), 'test_errors.py', 'exec'); print('test files syntax OK')"</automated>
  </verify>
  <done>pytest is installed. tests/ directory created with conftest.py (app + client fixtures using TestingConfig), test_main.py (test_homepage for MAIN-01), test_layout.py (test_base_template_has_bootstrap for LAYOUT-01 + test_viewport_meta for MOB-03), test_errors.py (test_404_page for LAYOUT-02). All test files are syntactically valid and importable. Tests will FAIL until implementation code exists — that is expected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem→git | .env file must not be committed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | .env file / config.py | mitigate | `.env` in `.gitignore` (Task 1). `SECRET_KEY` from `os.environ.get()` with dev fallback only (Task 2). `.env.example` shows template without secrets (Task 1). |
| T-01-02 | Tampering | pip installs | mitigate | slopcheck audit passed for flask and python-dotenv (RESEARCH.md §Package Legitimacy Audit). Both packages are [ASSUMED] — universally recognized, millions of downloads. Version pins in requirements.txt prevent supply-chain drift. |
| T-01-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] packages (not needed — all packages [ASSUMED] with known provenance). See RESEARCH.md Package Legitimacy Audit. |
</threat_model>

<verification>
Run the full test suite (expected to fail until implementation exists): `python -m pytest tests/ -v`
</verification>

<success_criteria>
- [ ] requirements.txt exists with flask==3.1.3 and python-dotenv==1.2.2
- [ ] .gitignore covers venv, instance, .env, __pycache__, .planning/
- [ ] .env.example shows SECRET_KEY template
- [ ] config.py has Config, DevelopmentConfig, ProductionConfig, TestingConfig classes
- [ ] pytest is installed and importable
- [ ] tests/conftest.py exists with app and client fixtures
- [ ] tests/test_main.py, test_layout.py, test_errors.py exist and import cleanly
</success_criteria>

<output>
Create `.planning/phases/01-project-skeleton/01-01-SUMMARY.md` when done
</output>
