---
phase: 02-auth-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - config.py
  - .env.example
  - app/models/__init__.py
  - app/models/user.py
autonomous: true
requirements: [AUTH-05]
must_haves:
  truths:
    - "The Flask application starts and all auth dependencies load without import errors"
    - "Session cookies are HttpOnly, SameSite=Lax, and 30-day remember-me is configured"
    - "User accounts can be stored with secure password hashing and three role levels"
  artifacts:
    - path: "app/models/user.py"
      provides: "User model with 7 columns, Role enum, set_password, check_password"
      contains: "class User"
    - path: "app/models/__init__.py"
      provides: "Clean package-level User import"
      exports: ["User"]
    - path: "requirements.txt"
      provides: "All 11 packages with pinned versions"
      contains: "Flask-SQLAlchemy"
    - path: "config.py"
      provides: "Auth config keys: SESSION_COOKIE_HTTPONLY, REMEMBER_COOKIE_DURATION, RATELIMIT_DEFAULT, MAIL_*"
      contains: "SESSION_COOKIE_HTTPONLY"
    - path: ".env.example"
      provides: "SMTP/MAIL env var documentation"
      contains: "MAIL_SERVER"
  key_links:
    - from: "User.check_password"
      to: "werkzeug.security.check_password_hash"
      via: "method call"
    - from: "User.role"
      to: "Role enum"
      via: "SQLAlchemy Enum column, default=Role.USER"
    - from: "User"
      to: "db.Model + UserMixin"
      via: "class inheritance"
---

<objective>
Install auth packages, add auth config keys, and create the User model with three-tier role enum.
This is the foundation all other Phase 2 plans build on — packages must be present, config keys defined, and User model available before create_app() wiring or auth routes can exist.

Purpose: Establish the dependency layer (packages), configuration layer (config keys), and data layer (User model) in one Wave 1 plan.
Output: Updated requirements.txt, config.py, .env.example; new app/models/ package with User model and Role enum.
</objective>

<execution_context>
@C:/Users/Waldo/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Waldo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-auth-system/02-CONTEXT.md
@.planning/phases/02-auth-system/02-RESEARCH.md
@.planning/phases/01-project-skeleton/01-CONTEXT.md
@app/extensions.py
@config.py
@requirements.txt
@.env.example
</context>

<interfaces>
<!-- Key contracts the executor needs from existing code -->
From app/extensions.py:
- `db` (SQLAlchemy instance) — User model inherits from `db.Model`
- `login_manager` (LoginManager instance) — User model inherits from `UserMixin` for Flask-Login integration

From config.py:
- `Config` base class — new config keys go here
- `DevelopmentConfig`, `ProductionConfig`, `TestingConfig` — subclass Config
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Install auth packages and update dependency files</name>
  <read_first>
@requirements.txt
@.env.example
  </read_first>
  <files>
requirements.txt
.env.example
  </files>
  <action>
Install the 7 auth packages into the project venv using pip. Update requirements.txt with pinned versions and .env.example with SMTP/MAIL env vars.

Packages to install (per RESEARCH.md verified versions): Flask-SQLAlchemy==3.1.1, Flask-Login==0.6.3, Flask-WTF==1.3.0, Flask-Mail==0.10.0, Flask-Migrate==4.1.0, Flask-Limiter==4.1.1, email-validator==2.3.0. Also install itsdangerous==2.2.0 (token signing) and Alembic==1.18.4 (migration engine, pulled by Flask-Migrate).

Update requirements.txt: replace current content with all packages on separate lines with pinned versions, sorted alphabetically (Alembic first, then email-validator, Flask, Flask-Limiter, Flask-Login, Flask-Mail, Flask-Migrate, Flask-SQLAlchemy, Flask-WTF, itsdangerous, python-dotenv, Werkzeug). Werkzeug 3.1.8 ships with Flask — pin explicitly.

Update .env.example: add after existing DATABASE_URL line — MAIL_SERVER=smtp.example.com, MAIL_PORT=587, MAIL_USE_TLS=true, MAIL_USERNAME=your-email@example.com, MAIL_PASSWORD=your-email-password, MAIL_DEFAULT_SENDER=noreply@example.com.

Install: `pip install Flask-SQLAlchemy==3.1.1 Flask-Login==0.6.3 Flask-WTF==1.3.0 Flask-Mail==0.10.0 Flask-Migrate==4.1.0 Flask-Limiter==4.1.1 email-validator==2.3.0 itsdangerous==2.2.0 Alembic==1.18.4`. Verify: `pip freeze | grep -E "Flask-SQLAlchemy|Flask-Login|Flask-WTF|Flask-Mail|Flask-Migrate|Flask-Limiter|email-validator|itsdangerous|Alembic"` shows all 9 packages.
  </action>
  <verify>
    <automated>pip freeze | grep -cE "Flask-SQLAlchemy|Flask-Login|Flask-WTF|Flask-Mail|Flask-Migrate|Flask-Limiter|email-validator|itsdangerous|Alembic" returns at least 9</automated>
  </verify>
  <acceptance_criteria>
1. `pip freeze` shows Flask-SQLAlchemy 3.1.1, Flask-Login 0.6.3, Flask-WTF 1.3.0, Flask-Mail 0.10.0, Flask-Migrate 4.1.0, Flask-Limiter 4.1.1, email-validator 2.3.0, itsdangerous 2.2.0, Alembic 1.18.4 installed
2. requirements.txt lists all 11 packages (9 new + Flask + python-dotenv) with pinned versions, alphabetically sorted
3. .env.example contains MAIL_SERVER, MAIL_PORT, MAIL_USE_TLS, MAIL_USERNAME, MAIL_PASSWORD, MAIL_DEFAULT_SENDER lines
  </acceptance_criteria>
  <done>All 9 auth packages installed in venv; requirements.txt and .env.example updated</done>
</task>

<task type="auto">
  <name>Task 2: Add auth and session config keys</name>
  <read_first>
@config.py
  </read_first>
  <files>config.py</files>
  <action>
Add all auth, session, mail, rate-limiting, and Flask-Migrate config keys to the Config base class and environment-specific subclasses.

In Config base class, add after WTF_CSRF_ENABLED line:
- `MAIL_SERVER = os.environ.get('MAIL_SERVER', 'localhost')`
- `MAIL_PORT = int(os.environ.get('MAIL_PORT', 25))`
- `MAIL_USE_TLS = os.environ.get('MAIL_USE_TLS', 'false').lower() == 'true'`
- `MAIL_USERNAME = os.environ.get('MAIL_USERNAME', None)`
- `MAIL_PASSWORD = os.environ.get('MAIL_PASSWORD', None)`
- `MAIL_DEFAULT_SENDER = os.environ.get('MAIL_DEFAULT_SENDER', 'noreply@flaskstuct.local')`
- `MAIL_SUPPRESS_SEND = False`
- `SESSION_COOKIE_HTTPONLY = True`
- `SESSION_COOKIE_SAMESITE = 'Lax'`
- `REMEMBER_COOKIE_HTTPONLY = True`
- `REMEMBER_COOKIE_DURATION = 2592000`  (30 days in seconds, per D-13)
- `RATELIMIT_DEFAULT = '10 per minute'` (per D-30)
- `RATELIMIT_STORAGE_URI = 'memory://'`

In ProductionConfig, add: `SESSION_COOKIE_SECURE = True`

In TestingConfig, add: `RATELIMIT_ENABLED = False` (prevents 429 in integration tests per RESEARCH.md Pitfall 6)

In DevelopmentConfig, add: `MAIL_SUPPRESS_SEND = False` (explicit — D-05 says dev prints to console, not suppress; the route handler checks config and conditionally prints vs sends).

IMPORTANT: Do NOT add DevelopmentConfig `db.create_all()` here — that goes in create_app() (Plan 02). Do NOT duplicate existing keys.
  </action>
  <verify>
    <automated>python -c "from config import Config, DevelopmentConfig, ProductionConfig, TestingConfig; assert Config.SESSION_COOKIE_HTTPONLY == True; assert Config.RATELIMIT_DEFAULT == '10 per minute'; assert ProductionConfig.SESSION_COOKIE_SECURE == True; assert TestingConfig.RATELIMIT_ENABLED == False; print('All config assertions passed')"</automated>
  </verify>
  <acceptance_criteria>
1. Config base class has MAIL_SERVER, MAIL_PORT, MAIL_USE_TLS, MAIL_USERNAME, MAIL_PASSWORD, MAIL_DEFAULT_SENDER, MAIL_SUPPRESS_SEND keys
2. Config base class has SESSION_COOKIE_HTTPONLY=True, SESSION_COOKIE_SAMESITE='Lax', REMEMBER_COOKIE_HTTPONLY=True, REMEMBER_COOKIE_DURATION=2592000
3. Config base class has RATELIMIT_DEFAULT='10 per minute', RATELIMIT_STORAGE_URI='memory://'
4. ProductionConfig has SESSION_COOKIE_SECURE=True
5. TestingConfig has RATELIMIT_ENABLED=False
6. `python -c "import config"` succeeds without errors
  </acceptance_criteria>
  <done>All config keys added to Config and subclasses; import succeeds</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create User model with Role enum and password methods</name>
  <read_first>
@app/extensions.py
@config.py
  </read_first>
  <files>
app/models/__init__.py
app/models/user.py
  </files>
  <behavior>
- Role enum exists with three values: USER='user', ADMIN='admin', SUPERUSER='superuser' (per D-11)
- User model inherits from UserMixin and db.Model, table name 'users' (per D-24)
- User.id is Integer PK auto-increment, User.email is String(255) unique indexed not-null, User.password_hash is String(255) not-null, User.role is Enum(Role) default USER not-null, User.is_active is Boolean default True not-null, User.created_at is DateTime default utcnow, User.updated_at is DateTime default utcnow with onupdate
- User.set_password(password) calls generate_password_hash
- User.check_password(password) calls check_password_hash and returns boolean
- User.__repr__ returns f'<User {self.email}>'
- app/models/__init__.py imports User for clean access elsewhere (per D-26)
  </behavior>
  <action>
Create app/models/ directory (if missing) with __init__.py and user.py.

app/models/__init__.py: import User from app.models.user, expose as package-level import.

app/models/user.py: define Role enum (values user/admin/superuser per D-11), User class per D-24 with columns: id (Integer, primary_key), email (String(255), unique=True, index=True, nullable=False), password_hash (String(255), nullable=False), role (Enum(Role), default=Role.USER, nullable=False), is_active (Boolean, default=True, nullable=False), created_at (DateTime, default=lambda: datetime.now(timezone.utc)), updated_at (DateTime, default=lambda: datetime.now(timezone.utc), onupdate=lambda: datetime.now(timezone.utc)). Imports: datetime.datetime/timezone, app.extensions.db, flask_login.UserMixin, werkzeug.security.generate_password_hash/check_password_hash, enum.Enum.

Methods: set_password(self, password) — sets self.password_hash = generate_password_hash(password). check_password(self, password) — returns check_password_hash(self.password_hash, password). __repr__ returns f'<User {self.email}>'.

Follow Pattern 2 from RESEARCH.md exactly for class structure and method signatures. Use `from app.extensions import db` (not `from app import db`).

DO NOT: create migration scripts here (Plan 02 handles Flask-Migrate wiring). DO NOT: add display_name, last_login, or any deferred fields. DO NOT: add relationship definitions (no related models exist yet).
  </action>
  <verify>
    <automated>python -c "from app.models import User; from app.models.user import Role; u = User(email='test@example.com'); u.set_password('Secret123!'); assert u.check_password('Secret123!'); assert not u.check_password('wrong'); assert u.role == Role.USER; assert u.is_active == True; assert u.email == 'test@example.com'; print('User model verified')"</automated>
  </verify>
  <acceptance_criteria>
1. `app/models/__init__.py` exists and exports User
2. `app/models/user.py` exists with Role enum (USER/ADMIN/SUPERUSER) and User class
3. User has columns: id, email (unique, indexed), password_hash, role (default USER), is_active (default True), created_at, updated_at
4. User.set_password() uses werkzeug generate_password_hash
5. User.check_password() uses werkzeug check_password_hash and returns boolean
6. `python -c "from app.models import User"` succeeds without import errors
7. `python -c "from app.models.user import Role; print(Role.USER.value)"` prints 'user'
  </acceptance_criteria>
  <done>User model created with Role enum, password hashing, and clean model-level import</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Config → App | Config keys define security defaults consumed by Flask-Login, Flask-Limiter, Flask-Mail |
| User model → Database | User data including password hash stored via SQLAlchemy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09 | Information Disclosure | User.password_hash storage | mitigate | Werkzeug pbkdf2:sha256 with per-password salt via generate_password_hash — not plaintext, not single-round (per D-24, RESEARCH.md §6) |
| T-02-13 | Elevation of Privilege | User.role default | mitigate | Role enum defaults to USER server-side; no client-controlled role assignment (per D-09) |
| T-02-SC | Tampering | pip install of 9 packages | accept | All 9 packages slopcheck-verified [OK] on PyPI per RESEARCH.md Package Legitimacy Audit; no [SUS] or [SLOP] packages detected |
</threat_model>

<verification>
Run after all tasks complete:
- `pip freeze | grep -cE "Flask-SQLAlchemy|Flask-Login|Flask-WTF|Flask-Mail|Flask-Migrate|Flask-Limiter|email-validator|itsdangerous|Alembic"` returns 9
- `python -c "from config import Config, DevelopmentConfig, ProductionConfig, TestingConfig; assert Config.SESSION_COOKIE_HTTPONLY == True; assert Config.REMEMBER_COOKIE_DURATION == 2592000; assert TestingConfig.RATELIMIT_ENABLED == False; print('OK')"` prints OK
- `python -c "from app.models import User; from app.models.user import Role; u = User(email='t@t.com'); u.set_password('Test1234!'); assert u.check_password('Test1234!'); print('OK')"` prints OK
</verification>

<success_criteria>
- [ ] 9 auth packages installed in venv with pinned versions
- [ ] requirements.txt updated with all 11 packages
- [ ] .env.example contains 6 MAIL_* env var lines
- [ ] config.py has all session, mail, rate-limit, and migrate config keys
- [ ] app/models/__init__.py exports User
- [ ] app/models/user.py has Role enum (3 values) and User model (7 columns, set_password, check_password, __repr__)
- [ ] `python -c "from app.models import User"` succeeds
</success_criteria>

<output>
Create `.planning/phases/02-auth-system/02-01-SUMMARY.md` when done
</output>
