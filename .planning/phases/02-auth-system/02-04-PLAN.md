---
phase: 02-auth-system
plan: 04
type: execute
wave: 4
depends_on: [02-02, 02-03]
files_modified:
  - app/auth/routes.py
  - app/__init__.py
  - app/models/user.py
  - app/main/routes.py
  - app/templates/dashboard.html
  - app/commands.py
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04, AUTH-05]
must_haves:
  truths:
    - "User can register and is auto-logged in with role=USER"
    - "User can login with session persistence and 'remember me' support"
    - "User can logout from /auth/logout"
    - "User can request and complete password reset via email token"
    - "Admin users can be created/promoted via Flask CLI"
    - "Unauthenticated users are redirected to /auth/login when accessing /dashboard"
  artifacts:
    - path: "app/auth/routes.py"
      provides: "8 route handlers across 2 tasks: login, register, logout (Task 1) + forgot-password, reset-password (Task 2)"
      exports: ["generate_reset_token", "verify_reset_token", "is_safe_url"]
    - path: "app/main/routes.py"
      provides: "Dashboard stub route protected by @login_required"
      contains: "@login_required"
    - path: "app/templates/dashboard.html"
      provides: "Post-login landing page with welcome message and empty state"
      contains: "Getting Started"
    - path: "app/commands.py"
      provides: "Flask CLI: create-admin, set-admin with interactive prompts"
      contains: "@app.cli.command"
  key_links:
    - from: "POST /auth/login"
      to: "User.query + check_password + login_user + is_safe_url redirect"
      via: "form.validate_on_submit() flow"
      pattern: "login_user\\(user, remember="
    - from: "POST /auth/register"
      to: "User(email, role=Role.USER) + set_password + db.session.commit + login_user"
      via: "form.validate_on_submit() flow per D-09/D-17"
      pattern: "role=Role\\.USER"
    - from: "POST /auth/forgot-password"
      to: "generate_reset_token + console/SMTP + reset_request_sent.html"
      via: "conditional config check per D-05/D-06"
    - from: "POST /auth/reset-password/<token>"
      to: "verify_reset_token + set_password + invalidate token + redirect login"
      via: "one-time token enforcement per D-07/D-08"
    - from: "@login_required on /dashboard"
      to: "login_manager.login_view = 'auth.login'"
      via: "Flask-Login redirect with next= param per D-14"
---

<objective>
Implement all auth route handlers (login, register, logout, forgot-password, reset-password), the dashboard stub route, and Flask CLI admin commands. This is the behavior layer that connects forms (Plan 03) and wiring (Plan 02) to deliver the full auth flow.

Purpose: After this plan, users can register, login, logout, reset passwords, and admins can be created via CLI.
Output: app/auth/routes.py, updated app/main/routes.py, app/templates/dashboard.html, app/commands.py
</objective>

<execution_context>
@C:/Users/Waldo/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Waldo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-auth-system/02-CONTEXT.md
@.planning/phases/02-auth-system/02-RESEARCH.md
@.planning/phases/02-auth-system/02-UI-SPEC.md
@.planning/phases/01-project-skeleton/01-04-SUMMARY.md
@app/__init__.py
@app/auth/__init__.py
@app/auth/forms.py
@app/models/user.py
@app/templates/base.html
@app/templates/auth/base.html
@app/main/routes.py
@config.py
</context>

<interfaces>
<!-- Key contracts for executor -->
From app/auth/__init__.py:
- `auth_bp` Blueprint with url_prefix='/auth' — all auth routes decorated with @auth_bp.route()

From app/auth/forms.py (created in Plan 03):
- `LoginForm`, `RegistrationForm`, `ForgotPasswordForm`, `ResetPasswordForm`

From app/models/user.py (created in Plan 01):
- `User` model: User.query.filter_by(email=...).first(), User(email=..., role=..., ...), db.session.add/commit
- `Role` enum: Role.USER, Role.ADMIN, Role.SUPERUSER
- `user.set_password(password)`, `user.check_password(password)`, `user.is_active`

From app/main/__init__.py:
- `main_bp` Blueprint — @main_bp.route('/dashboard') for dashboard stub

From flask_login (available via installed packages):
- `login_user(user, remember=bool)`, `logout_user()`, `current_user`, `@login_required`
- `LoginManager.user_loader` (already registered in Plan 02)

From flask_limiter (available via installed packages):
- `Limiter` instance — rate-limit decorators
- `get_remote_address` — key function

From itsdangerous (available via installed packages):
- `URLSafeTimedSerializer` — token generation and verification
- `SignatureExpired`, `BadSignature` — exception handling
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create core auth route handlers (login, register, logout) with helpers and back-modifications</name>
  <read_first>
@app/auth/__init__.py
@app/auth/forms.py
@app/models/user.py
@app/__init__.py
@config.py
@.planning/phases/02-auth-system/02-CONTEXT.md
@.planning/phases/02-auth-system/02-UI-SPEC.md
  </read_first>
  <files>
app/auth/routes.py
app/__init__.py
app/models/user.py
  </files>
  <behavior>
- GET /auth/login: if current_user.is_authenticated, redirect to dashboard with flash "You are already logged in." (info). Else render login.html with LoginForm.
- POST /auth/login: rate limited 10/min. Validate form. Lookup user by normalized email. Check password. Check is_active. login_user with remember from form. Redirect to next (safe URL check) or dashboard. Flash "Invalid email or password." (danger) on failure.
- GET /auth/register: if authenticated, redirect dashboard. Else render register.html.
- POST /auth/register: rate limited. Validate form. Create User with role=Role.USER (D-09), set_password, db.session.add+commit. Auto-login (D-17). Redirect dashboard.
- GET /auth/logout: logout_user(), redirect to main.index (D-18).
- Helper functions: is_safe_url (open redirect prevention per D-19), get_limiter (accesses current_app.extensions limiter; no-op fallback for TestingConfig), generate_reset_token and verify_reset_token (itsdangerous URLSafeTimedSerializer with salt='password-reset', 1-hour expiry).
- Rate limiting: 10 per minute on login and register POST endpoints (D-29/D-30). Use get_limiter() helper and limiter.limit("10 per minute") decorator. On breach: catch RateLimitExceeded and flash "Too many attempts. Please try again in X seconds." (warning) + re-render form (D-31).
- Back-modify app/models/user.py: add `password_reset_requested_at = db.Column(db.DateTime, nullable=True)` to User model for token one-time-use tracking (D-07).
- Back-modify app/__init__.py: add custom 429 error handler that flashes rate-limit message and redirects to request.url (D-31).
  </behavior>
  <action>
Create app/auth/routes.py with core auth route handlers and all helper functions. This task covers login, register, and logout — the three routes that form the primary auth UX. Password reset flow goes in Task 2.

Imports needed: flask (Blueprint is not needed here — use auth_bp from __init__), render_template, redirect, url_for, flash, request, current_app; flask_login (login_user, logout_user, current_user, login_required); app.auth.forms (LoginForm, RegistrationForm, ForgotPasswordForm, ResetPasswordForm); app.models.user (User, Role); app.extensions (db); itsdangerous (URLSafeTimedSerializer, SignatureExpired, BadSignature); urllib.parse (urlparse, urljoin); datetime.

Route structure — use `from app.auth import auth_bp` then decorate:

**Helper functions (module-level, before routes):**
- `is_safe_url(target)`: validates redirect target is same host. urlparse(urljoin(request.host_url, target)).netloc == urlparse(request.host_url).netloc (prevents open redirect, D-19).
- `get_limiter()`: returns current_app.extensions.get('limiter'). If None (rate limiting disabled in TestingConfig), returns a no-op object with a `.limit()` method that returns a pass-through decorator.
- `generate_reset_token(user)`: s = URLSafeTimedSerializer(current_app.config['SECRET_KEY']); return s.dumps(str(user.id), salt='password-reset')
- `verify_reset_token(token, max_age=3600)`: try s.loads(token, salt='password-reset', max_age=max_age); return User.query.get(int(user_id)). Catch SignatureExpired → return None; BadSignature → return None.

**Login routes (GET/POST):**
- GET: if current_user.is_authenticated, flash "You are already logged in." (info), redirect to dashboard. Else render login.html with LoginForm().
- POST: apply rate limit via `@rate_limit()`. if form.validate_on_submit(): email = form.email.data.strip().lower(); user = User.query.filter_by(email=email).first(); if user and user.check_password(form.password.data): if not user.is_active: flash "This account has been deactivated." (danger), render login.html; login_user(user, remember=form.remember.data); next_page = request.args.get('next'); if next_page and is_safe_url(next_page): return redirect(next_page); return redirect(url_for('main.dashboard')); flash "Invalid email or password." (danger). Render login.html.

**Register routes (GET/POST):**
- GET: if authenticated, redirect dashboard. Else render register.html.
- POST: rate limit. if form.validate_on_submit(): user = User(email=form.email.data.strip().lower(), role=Role.USER) (D-09); user.set_password(form.password.data); db.session.add(user); db.session.commit(); login_user(user, remember=False) (D-17 — no remember on registration); flash "Account created successfully. Welcome!" (success); return redirect(url_for('main.dashboard')). Render register.html on failure.

**Logout:**
- GET: logout_user(); return redirect(url_for('main.index')) (D-18).

Rate limit decorator pattern: since limiter is in current_app.extensions, define wrapper:
```python
def rate_limit():
    limiter = current_app.extensions.get('limiter')
    if limiter:
        return limiter.limit("10 per minute")
    return lambda f: f  # no-op if limiter disabled (TestingConfig)
```
Apply: `@rate_limit()` on POST handlers before the route handler body.

**Back-modify app/models/user.py:** Add password reset tracking column to User model. After existing columns, add:
```python
password_reset_requested_at = db.Column(db.DateTime, nullable=True)
```
This is needed for one-time token enforcement (D-07). The column stores a timestamp when a reset token is generated; after successful reset it's set to None.

**Back-modify app/__init__.py:** Add a custom 429 error handler for Flask-Limiter rate-limit breaches. In create_app(), after existing error handler registrations, add:
```python
@app.errorhandler(429)
def ratelimit_error(e):
    flash(f"Too many attempts. Please try again in {e.retry_after} seconds.", 'warning')
    return redirect(request.url)
```
This fulfills D-31: rate limit exceeded → flash message and form re-rendered, not a separate 429 error page.

Flash categories: login failure=danger, register success=success, logout=info, already_logged_in=info, please_log_in=warning, rate_limit=warning (per UI-SPEC).

DO NOT: implement forgot-password or reset-password routes here (Task 2). DO NOT: use Flask-Security-Too. DO NOT: add email verification flow. DO NOT: implement OAuth.
  </action>
  <verify>
    <automated>python -c "from app import create_app; app = create_app('testing'); client = app.test_client(); r = client.get('/auth/login'); assert r.status_code == 200; r = client.get('/auth/register'); assert r.status_code == 200; r = client.get('/auth/logout'); assert r.status_code == 302; print('Core auth endpoints respond')"</automated>
  </verify>
  <acceptance_criteria>
1. GET /auth/login returns 200 with login.html (D-01/D-02 shell)
2. POST /auth/login with valid credentials redirects to /dashboard (D-16) and sets session cookie
3. POST /auth/login with invalid credentials re-renders login with flash "Invalid email or password." (danger)
4. POST /auth/login with deactivated account re-renders login with flash "This account has been deactivated." (danger) (D-25)
5. POST /auth/login with "remember me" sets long-lived session (D-12/D-13)
6. POST /auth/login with `?next=/somepage` redirects to that page after login (D-19), validated by is_safe_url()
7. GET /auth/register returns 200
8. POST /auth/register creates user with role=Role.USER (D-09), auto-login (D-17), redirects to /dashboard
9. POST /auth/register with duplicate email shows "Email already registered." error (D-28)
10. POST /auth/register with weak password (< 8 chars or < 2 char types) shows validation error (D-20)
11. GET /auth/logout clears session and redirects to / (D-18)
12. Rate limiting active on login and register POST endpoints (D-29/D-30)
13. Custom 429 handler registered in create_app() flashes warning and redirects (D-31)
14. `python -c "from app.auth.routes import is_safe_url; assert is_safe_url('/dashboard'); assert not is_safe_url('http://evil.com')"` passes
15. Email normalization: login and register strip whitespace + lowercase email (D-27)
16. User model has `password_reset_requested_at` column added
17. Core helper functions (is_safe_url, get_limiter, generate_reset_token, verify_reset_token) importable from routes
18. `python -c "from app.auth.routes import generate_reset_token, verify_reset_token"` succeeds
  </acceptance_criteria>
  <done>Core auth routes working: register, login, logout with helper functions, rate limiting, 429 handler, and password_reset_requested_at column</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create password reset route handlers with email dispatch and token enforcement</name>
  <read_first>
@app/auth/__init__.py
@app/auth/forms.py
@app/models/user.py
@app/__init__.py
@config.py
@.planning/phases/02-auth-system/02-CONTEXT.md
@.planning/phases/02-auth-system/02-UI-SPEC.md
  </read_first>
  <files>app/auth/routes.py</files>
  <behavior>
- GET /auth/forgot-password: render forgot_password.html with ForgotPasswordForm.
- POST /auth/forgot-password: rate limited. Normalize email. If user found: generate token via generate_reset_token(user), set user.password_reset_requested_at = utcnow, commit. Dev mode (DEBUG and not TESTING): print reset URL to console. Prod: send email via Flask-Mail with try/except for SMTP errors. Always render reset_request_sent.html (D-06: same response whether email found or not — prevents user enumeration).
- GET /auth/reset-password/<token>: verify token. If valid, render reset_password.html with ResetPasswordForm. If expired, flash "This reset link has expired. Please request a new one." (danger) and redirect to forgot_password. If invalid (BadSignature), flash "This reset link is invalid. Please request a new one." (danger) and redirect to forgot_password.
- POST /auth/reset-password/<token>: rate limited. Verify token again. If valid and form.validate_on_submit(): user.set_password(form.password.data); user.password_reset_requested_at = None (invalidate token — one-time use per D-07); db.session.commit(); flash "Password reset successfully. Please log in." (success); redirect to auth.login (D-08: no auto-login after reset). Render reset_password.html on failure.
- One-time token enforcement (D-07): password_reset_requested_at column stores timestamp when token was generated. Token encodes user.id + generation_time. On verify: decode token, get user_id. Check user.password_reset_requested_at is not None. On successful reset: set to None — subsequent attempts with same token fail.
- Rate limiting: 10 per minute on forgot-password and reset-password POST endpoints (D-29/D-30). Use `@rate_limit()` decorator (same pattern as Task 1).
  </behavior>
  <action>
Continue building app/auth/routes.py — append forgot-password and reset-password route handlers using the auth_bp blueprint and helper functions created in Task 1.

**Forgot password routes (GET/POST):**
- GET: render forgot_password.html with ForgotPasswordForm().
- POST: `@rate_limit()`. email = form.email.data.strip().lower(); user = User.query.filter_by(email=email).first(); if user: token = generate_reset_token(user); user.password_reset_requested_at = datetime.now(timezone.utc); db.session.commit(); if current_app.config.get('DEBUG') and not current_app.config.get('TESTING'): reset_url = url_for('auth.reset_password', token=token, _external=True); print(f"Password reset URL for {user.email}: {reset_url}"); else: construct Flask-Mail Message, send via current_app.extensions['mail'].send(msg), wrapped in try/except for SMTP failures (log error, still render confirmation — don't 500). Always render reset_request_sent.html (D-06 — same response whether email found or not). If user not found, skip token generation and still render reset_request_sent.html.

**Reset password routes (GET/POST):**
- GET: user = verify_reset_token(token); if user is None or user.password_reset_requested_at is None: flash appropriate error ("This reset link has expired. Please request a new one." or "This reset link is invalid. Please request a new one." — danger category), redirect to auth.forgot_password. Render reset_password.html with ResetPasswordForm().
- POST: `@rate_limit()`. user = verify_reset_token(token); if user is None or user.password_reset_requested_at is None: same error handling as GET. if form.validate_on_submit(): user.set_password(form.password.data); user.password_reset_requested_at = None (one-time invalidation per D-07); db.session.commit(); flash "Password reset successfully. Please log in." (success); return redirect(url_for('auth.login')) (D-08 — no auto-login). Render reset_password.html on form failure.

**Dev email printing (D-05):** When app.debug is True and not TESTING, construct reset_url = url_for('auth.reset_password', token=token, _external=True), print(f"Password reset URL: {reset_url}") to console. Production path: construct Flask-Mail Message with subject, sender from MAIL_DEFAULT_SENDER, recipients=[user.email], body containing the reset URL. Wrap mail.send(msg) in try/except for SMTP failures — log the error (print to stderr), still render confirmation page (don't 500 on email failure).

Flash categories: reset_success=success, token_expired=danger, token_invalid=danger (per UI-SPEC).

DO NOT: modify core routes from Task 1. DO NOT: add email verification flow. DO NOT: implement OAuth.
  </action>
  <verify>
    <automated>python -c "from app import create_app; app = create_app('testing'); client = app.test_client(); r = client.get('/auth/forgot-password'); assert r.status_code == 200; print('Forgot-password endpoint responds')"</automated>
  </verify>
  <acceptance_criteria>
1. GET /auth/forgot-password returns 200 with forgot_password.html
2. POST /auth/forgot-password renders reset_request_sent.html for both existing and non-existing emails (D-06 — prevents user enumeration)
3. POST /auth/forgot-password with existing email: generates token, stores password_reset_requested_at, prints URL to console in dev (D-05)
4. POST /auth/forgot-password with non-existing email: renders same confirmation page, no token generated
5. GET /auth/reset-password/<valid_token> returns 200 with reset_password.html and ResetPasswordForm
6. GET /auth/reset-password/<expired_token> flashes "This reset link has expired." (danger) and redirects to forgot_password
7. GET /auth/reset-password/<invalid_token> flashes "This reset link is invalid." (danger) and redirects to forgot_password
8. POST /auth/reset-password/<valid_token> updates password_hash, sets password_reset_requested_at=None (one-time invalidation per D-07), flashes success, redirects to /auth/login (D-08: no auto-login)
9. POST /auth/reset-password/<valid_token> with token reuse: second attempt fails (password_reset_requested_at is None)
10. Rate limiting active on forgot-password and reset-password POST endpoints (D-29/D-30)
11. Dev mode: reset URL printed to console; prod mode: email dispatched via Flask-Mail
12. Email sending failure does not 500 — SMTP errors caught, logged, confirmation page still rendered
  </acceptance_criteria>
  <done>Password reset flow working: forgot-password with enumeration prevention, reset-password with token one-time use, dev console/prod email dispatch</done>
</task>

<task type="auto">
  <name>Task 3: Add dashboard stub route and template</name>
  <read_first>
@app/main/routes.py
@app/templates/base.html
@.planning/phases/02-auth-system/02-UI-SPEC.md
  </read_first>
  <files>
app/main/routes.py
app/templates/dashboard.html
  </files>
  <action>
Two parts:

**app/main/routes.py:** Add dashboard stub route after existing route handlers. Import: from flask_login import login_required, current_user. Route: @main_bp.route('/dashboard'); @login_required; def dashboard(): return render_template('dashboard.html', user=current_user). This is the protected post-login landing page (D-16). The @login_required decorator automatically handles unauthenticated redirect via login_manager.login_view='auth.login' (set in Plan 02) and passes `next` parameter per D-14/D-19.

**app/templates/dashboard.html:** Create a minimal template that extends base.html (NOT auth/base.html — this is the main app layout with sidebar). Per UI-SPEC:
- {% extends "base.html" %}, {% block title %}Dashboard — FlaskStuct{% endblock %}
- content block: h1 "Dashboard", p.lead "Welcome, {{ user.email }}" (welcome message), then empty state section per UI-SPEC.
- Empty state: div.text-center.py-5 > i.bi.bi-grid-1x2.display-1.text-muted.mb-3, h2 "Getting Started", p.text-muted "Your dashboard is ready. Additional features and widgets will appear here in future updates."
- This is a stub — full dashboard content comes in Phase 3. The stub proves authenticated access works.

DO NOT: add sidebar role-gating (Phase 3). DO NOT: add any interactive features beyond the welcome message and empty state. DO NOT: extend auth/base.html (this extends main base.html).
  </action>
  <verify>
    <automated>python -c "from app import create_app; app = create_app('testing'); client = app.test_client(); r = client.get('/dashboard', follow_redirects=True); assert r.status_code == 200; assert b'Log In' in r.data; print('Dashboard redirects unauthenticated user to login')"</automated>
  </verify>
  <acceptance_criteria>
1. app/main/routes.py has @main_bp.route('/dashboard') with @login_required decorator
2. GET /dashboard from unauthenticated client redirects to /auth/login?next=%2Fdashboard (D-14)
3. app/templates/dashboard.html extends base.html, shows "Dashboard" heading, "Welcome, {email}" lead, empty state with bi-grid-1x2 icon and "Getting Started" heading
4. Page title is "Dashboard — FlaskStuct"
5. `grep -c "@login_required" app/main/routes.py` returns >= 1
6. `grep -c "Getting Started" app/templates/dashboard.html` returns 1
  </acceptance_criteria>
  <done>Dashboard stub route protected by @login_required; extends base.html with welcome message and empty state</done>
</task>

<task type="auto">
  <name>Task 4: Create Flask CLI admin commands</name>
  <read_first>
@app/models/user.py
@.planning/phases/02-auth-system/02-CONTEXT.md
@run.py
  </read_first>
  <files>app/commands.py</files>
  <action>
Create app/commands.py with two Flask CLI commands per D-10 and RESEARCH.md Code Examples section.

File structure: import click, getpass (stdlib), create_app from app, db from app.extensions, User/Role from app.models.user. Create app = create_app() at module level (same pattern as run.py). Then define commands with @app.cli.command().

**flask create-admin:** Interactive prompt (Django createsuperuser style). Prompts: email = input('Email: ').strip().lower(); password = getpass('Password: '); confirm = getpass('Confirm password: '). If passwords don't match: click.echo(err=True) and return. Inside app.app_context(): check User.query.filter_by(email=email).first() — if exists, click.echo(f'User {email} already exists.', err=True). Else: user = User(email=email, role=Role.ADMIN); user.set_password(password); db.session.add(user); db.session.commit(); click.echo(f'Admin user {email} created.').

**flask set-admin:** Accepts email argument and --role option. @click.argument('email'); @click.option('--role', type=click.Choice(['user', 'admin', 'superuser']), default='admin'). Inside app.app_context(): user = User.query.filter_by(email=email.strip().lower()).first(); if not user: click.echo(f'User {email} not found.', err=True); return; user.role = Role(role); db.session.commit(); click.echo(f'User {email} role set to {role}.').

Password validation on create-admin: enforce D-20 policy — minimum 8 chars, 2 of 4 character types. If password fails, click.echo(err=True) and return. Use same regex pattern as RegistrationForm.

Also add --help text to commands via docstrings which click auto-renders.

DO NOT: register these as blueprints. DO NOT: create web UI for admin creation (per D-10, CLI only). DO NOT: add user listing or role management UI.
  </action>
  <verify>
    <automated>python -c "from app.commands import app; runner = app.test_cli_runner(); result = runner.invoke(args=['create-admin'], input='test@admin.com\nAdmin123!\nAdmin123!\n'); print(result.output)" 2>&1 | grep -c "Admin user"</automated>
  </verify>
  <acceptance_criteria>
1. app/commands.py exists with create_admin and set_admin CLI commands
2. `flask create-admin` interactively prompts for email, password (hidden via getpass), confirm password (D-10)
3. `flask create-admin` enforces D-20 password policy (min 8 chars, 2 of 4 types)
4. `flask create-admin` creates user with role=Role.ADMIN
5. `flask create-admin` rejects duplicate emails with error message
6. `flask set-admin EMAIL --role admin` promotes existing user to specified role
7. `flask set-admin EMAIL --role superuser` sets superuser role (D-11)
8. `flask set-admin EMAIL` with non-existent email shows error
9. `python -c "from app.commands import app; assert app.cli"` succeeds
10. Both commands use app.app_context() for DB access
  </acceptance_criteria>
  <done>CLI commands create-admin and set-admin working with interactive password input and role assignment</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → POST /auth/* | Untrusted input: email, password, token, CSRF token, next parameter |
| CLI → User model | Admin creation via flask create-admin/set-admin (trusted, but still validated) |
| App → SMTP / Console | Reset token URL crosses app boundary (email or console) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Elevation of Privilege | POST /auth/login | mitigate | Flask-Limiter 10 req/min/IP prevents brute force (D-29/D-30); on_breach handler flashes + re-renders form (D-31) |
| T-02-03 | Information Disclosure | POST /auth/forgot-password | mitigate | Same reset_request_sent.html rendered for existing AND non-existing emails (D-06); no timing difference |
| T-02-06 | Elevation of Privilege | GET/POST /auth/reset-password/<token> | mitigate | itsdangerous URLSafeTimedSerializer with 1-hour expiry; one-time use via password_reset_requested_at DB tracking (D-07); token invalidated after successful reset |
| T-02-07 | Spoofing | Login next= parameter | mitigate | is_safe_url() validates redirect target is same host using urlparse netloc comparison (D-19); rejects external URLs |
| T-02-08 | Tampering | User query in routes | accept | SQLAlchemy ORM with parameterized .filter_by() — no raw SQL; SQLi risk low |
| T-02-11 | Elevation of Privilege | POST /auth/register | mitigate | role=Role.USER hardcoded server-side (D-09); no role field on RegistrationForm; privilege escalation impossible via registration |
| T-02-18 | Information Disclosure | Dev console password reset URL | accept | Dev-only; D-05 explicitly requires console printing in DevelopmentConfig; production uses SMTP, no console leakage |
</threat_model>

<verification>
Run after all tasks:
- `python -c "from app import create_app; app = create_app('testing'); client = app.test_client(); print('GET /auth/login:', client.get('/auth/login').status_code); print('GET /auth/register:', client.get('/auth/register').status_code); print('GET /auth/logout:', client.get('/auth/logout').status_code); print('GET /auth/forgot-password:', client.get('/auth/forgot-password').status_code); print('GET /dashboard:', client.get('/dashboard', follow_redirects=True).status_code)"` — all print 200 or 302
- `python -c "from app.commands import app; assert app.cli; print('CLI registered')"` prints CLI registered
- `grep -c "is_safe_url" app/auth/routes.py` returns >= 1
- `grep -c "password-reset" app/auth/routes.py` returns >= 1 (token salt)
</verification>

<success_criteria>
- [ ] All 8 auth route handlers implemented (login GET/POST, register GET/POST, logout, forgot-password GET/POST, reset-password GET/POST)
- [ ] Rate limiting applied to all 4 POST endpoints with flash on breach (D-29/D-30/D-31)
- [ ] is_safe_url() prevents open redirect (D-19)
- [ ] Registration: role=USER, auto-login, redirect /dashboard (D-09/D-17)
- [ ] Login: is_active check, remember me, next= redirect (D-12/D-13/D-14/D-19/D-25)
- [ ] Logout: redirect to / (D-18)
- [ ] Forgot-password: generic response for found/not-found email (D-06); dev console print, prod SMTP (D-05)
- [ ] Reset-password: 1-hour token expiry, one-time use via DB tracking (D-07); redirect login on success (D-08)
- [ ] Email normalization (strip+lower) on login and registration (D-27)
- [ ] Dashboard stub route protected by @login_required, extends base.html with "Welcome, {email}" (D-16)
- [ ] flask create-admin creates admin user interactively (D-10)
- [ ] flask set-admin EMAIL --role promotes user to role (D-10/D-11)
- [ ] Password policy enforced in CLI create-admin (D-20)
</success_criteria>

<output>
Create `.planning/phases/02-auth-system/02-04-SUMMARY.md` when done
</output>
